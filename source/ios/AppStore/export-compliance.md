# Export compliance

- `ITSAppUsesNonExemptEncryption`: `false`
- Uses encryption: HTTPS/TLS provided by Apple's operating system networking APIs
- Uses proprietary or non-standard cryptography: No
- Exempt from export documentation upload: Yes, based on use of standard OS-provided HTTPS/TLS only

Reconfirm these answers if custom cryptography is added.

