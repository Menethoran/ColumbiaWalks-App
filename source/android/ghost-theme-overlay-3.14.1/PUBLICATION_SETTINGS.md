# Ghost publication settings for 3.14.1

Set the Ghost **Publication description** to exactly:

```text
Independent community initiative documenting pedestrian-safety concerns in Columbia, Pennsylvania.
```

This replaces the prior publication description, whose wording could imply a
relationship that ColumbiaWalks does not have. Change the setting through
Ghost Admin rather than editing generated HTML or the Ghost database.

After saving, verify the neutral description on the homepage metadata, Open
Graph and Twitter metadata, JSON-LD, RSS, `llms.txt`, and `llms-full.txt`.

Do not publish this overlay until the permanent-key-signed 3.14.1 APK is
available at `assets/downloads/ColumbiaWalks-3.14.1.apk` and has passed the
release verifier.
